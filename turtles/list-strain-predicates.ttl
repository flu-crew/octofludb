PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX f: <https://flucrew.org/term/>

# Lists and counts the types of edges associated with a strain 
SELECT ?p (COUNT(?p) as ?count)
WHERE {
    ?strain a f:strain_id .
    ?strain ?p ?o .
}
GROUP BY ?p
